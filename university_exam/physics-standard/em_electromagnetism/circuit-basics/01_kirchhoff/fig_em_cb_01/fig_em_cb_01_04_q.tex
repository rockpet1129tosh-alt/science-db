
\documentclass[border=1pt]{standalone}

\usepackage{tikz}

\usetikzlibrary{arrows.meta}

\usepackage{circuitikz}

\ctikzset{resistors/width=1.1, batteries/scale=1.2, 
    capacitors/width=0.2, capacitors/height=0.9,
    grounds/scale=1.6}
\ctikzset{bipoles/cuteswitch/thickness=0.23}

\begin{document}

\begin{circuitikz}

    \pgfmathsetmacro{\xa}{7}
    \pgfmathsetmacro{\xb}{1}
    \pgfmathsetmacro{\ya}{3}
    \pgfmathsetmacro{\yb}{1.3}

    \draw (0,0) to[battery1, invert, l_=E] ++ (-\xa,0)
    to [short, -o] ++ (0,\ya) node [anchor=east] {A} 
    to [short] ++ (\xb,0) coordinate (S)
    to [short] ++ (0,\yb)
    to [european resistor=$\mathrm{R}_1$, -*] ++ (\xa/2-\xb,0) coordinate (C)
    to [european resistor=$\mathrm{R}_2$] ++ (\xa/2-\xb,0)
    to [short] ++ (0,-\yb) coordinate (T);
    \draw (S) to [short] ++ (0,-\yb)
    to [european resistor=$\mathrm{R}_3$, -*] ++ (\xa/2-\xb,0) coordinate (D)
    to [european resistor=$\mathrm{R}_4$] ++ (\xa/2-\xb,0) -- (T)
    to [short, -o] ++ (\xb,0) node [anchor=west] {B} -- (0,0);
    \draw (C) to [european resistor=$\mathrm{R}_5$] (D);

\end{circuitikz}

\end{document}
