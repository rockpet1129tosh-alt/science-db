
\documentclass[border=1pt]{standalone}

\usepackage{tikz}

\usetikzlibrary{arrows.meta}

\usepackage{circuitikz}

\ctikzset{resistors/width=1.1, batteries/scale=1.2, 
    capacitors/width=0.2, capacitors/height=0.9,
    grounds/scale=1.6}
\ctikzset{bipoles/cuteswitch/thickness=0.23}

\begin{document}

\begin{circuitikz}

    \pgfmathsetmacro{\xa}{7}
    \pgfmathsetmacro{\xb}{3.1}
    \pgfmathsetmacro{\ya}{3}
    \pgfmathsetmacro{\yb}{1}

    \draw (0,0) to[battery1, invert, l_=E] ++ (-\xa,0)
    to [short, -o] ++ (0,\ya/2) node [anchor=east] {A} 
    to [short] ++ (0,\ya/2)
    to [european resistor=$\mathrm{R}_1$, -*] ++ (\xb,0) coordinate (C)
    to [short] ++ (0,\yb)
    to [european resistor=$\mathrm{R}_2$] ++ (\xb,0)
    to [short, -*] ++ (0,-\yb) coordinate (D);
    \draw (C) to [short] ++ (0,-\yb)
    to [european resistor=$\mathrm{R}_3$] ++ (\xb,0) -- (D) -- (0,\ya)
    to [short, -o] ++ (0,-\ya/2) node [anchor=west] {B} -- (0,0);

\end{circuitikz}

\end{document}
