# science-db 引き継ぎメモ（2026-03-01）

このファイルは、別チャット・別担当が見ても作業状況をすぐ再開できるように残した記録です。

## 1. 実施済み事項

1. GitHub リポジトリ名を変更
   - 旧: `rockpet1129tosh-alt/physics-db`
   - 新: `rockpet1129tosh-alt/science-db`

2. ローカル側の連動
   - `origin` を新URLへ更新済み
   - 新フォルダを作成: `C:\Users\selec\Documents\tex_all\science-db`
   - 旧フォルダから未コミット変更を新フォルダへコピー済み

3. 旧フォルダ削除
   - `C:\Users\selec\Documents\tex_all\physics-db` は削除済み
   - ロック元だった `yazi.exe` を停止して削除完了

## 2. 現在の作業ディレクトリ

- `C:\Users\selec\Documents\tex_all\science-db`

## 3. 現在のGit状態（引き継ぎ時点）

`git status -sb` の主な内容:

- `M university_exam/physics-standard/ps_a.log`
- `M university_exam/physics-standard/ps_a.pdf`
- `M university_exam/physics-standard/ps_a.synctex.gz`
- `M university_exam/physics-standard/ps_q.log`
- `M university_exam/physics-standard/ps_q.pdf`
- `M university_exam/physics-standard/ps_q.synctex.gz`
- `?? scripts/README.md`

## 4. これから行う作業（ユーザー方針）

ユーザー指示: 「パスの修正はユーザー側で実施する」

対象の中心は `scripts/compile/*.ps1` と `scripts/migration/*.ps1`。
特に以下の固定値を点検:

- `C:\Users\selec\Documents\tex_all\physics-db`
- `physics-standard`（必要に応じて残す/置換を判断）
- `questions/answers/figures` 前提パス

## 5. 次チャット用の即実行コマンド

```powershell
cd C:\Users\selec\Documents\tex_all\science-db
git remote -v
git status -sb
```

必要なら固定パス検出:

```powershell
Get-ChildItem .\scripts -Recurse -Filter *.ps1 |
  Select-String -Pattern 'physics-db|C:\\Users\\selec\\Documents\\tex_all\\physics-db'
```

## 6. 注意

- `.pdf/.log/.synctex.gz` は生成物のため、コミット要否を先に判断する
- 大量置換前に `git checkout -b` で作業ブランチを切るのが安全